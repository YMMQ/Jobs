<p id="notice"><%= notice %></p>

<h1>Addvs</h1>

<table>
  <thead>
    <tr>
      <th>Addv title</th>
      <th>Image</th>
      <th>Newspaper</th>
      <th>City</th>
      <th>Organization</th>
      <th>Addv date</th>
      <th>Last date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @addvs.each do |addv| %>
      <tr>
        <td><%= addv.addv_title %></td>
        <td><%= addv.image %></td>
        <td><%= addv.newspaper %></td>
        <td><%= addv.city %></td>
        <td><%= addv.organization %></td>
        <td><%= addv.addv_date %></td>
        <td><%= addv.last_date %></td>
        <td><%= link_to 'Show', addv %></td>
        <td><%= link_to 'Edit', edit_addv_path(addv) %></td>
        <td><%= link_to 'Destroy', addv, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Addv', new_addv_path %>
