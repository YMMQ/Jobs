<%= form_for(t_degree) do |f| %>
  <% if t_degree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(t_degree.errors.count, "error") %> prohibited this t_degree from being saved:</h2>

      <ul>
      <% t_degree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
